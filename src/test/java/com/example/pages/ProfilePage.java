package com.example.pages;\n\nimport org.openqa.selenium.By;\nimport org.openqa.selenium.WebDriver;\n\n/**\n * Page Object for the Profile page to verify stored full name or other profile info\n */\npublic class ProfilePage extends BasePage {\n    private final By fullNameLabel = By.cssSelector(\".profile-fullname\");\n\n    public ProfilePage(WebDriver driver) {\n        super(driver);\n    }\n\n    public String getStoredFullName() {\n        if (driver.findElements(fullNameLabel).isEmpty()) return \"\";\n        return driver.findElement(fullNameLabel).getText().trim();\n    }\n}\n